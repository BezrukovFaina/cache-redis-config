name: Cache Redis Config

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - name: Print environment variables
        run: echo "Environment variables: ${{ secrets.GITHUB_TOKEN }}"
      - name: Build and test
        run: echo "Building and testing cache-redis-config"